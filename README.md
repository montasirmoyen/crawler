# crawler
A C++ program that crawls websites.

Initially created to explore multi-threading in a more applied way, beyond the Operating Systems class I'm taking this semester.

## Usage
### macOS
```
cmake -S . -B build
cmake --build build
./build/crawler <url> <limit>
```